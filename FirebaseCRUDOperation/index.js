/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import Firebase_Data from './src/Firebase/Firebase_Data';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Firebase_Data);
